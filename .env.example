# Copy to .env.local to use locally. Every variable is optional.

# Canonical origin for metadata, JSON-LD, robots.txt, sitemap.xml and llms.txt.
# Overrides Vercel's VERCEL_PROJECT_PRODUCTION_URL; falls back to
# http://localhost:3000 when neither is set.
# SITE_URL=https://abgupta.vercel.app
